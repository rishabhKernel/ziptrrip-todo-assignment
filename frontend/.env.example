# Frontend Environment Configuration Example
# Set to your deployed backend URL in production (e.g., https://taskflow-backend.onrender.com/api)
# Leave empty or unset in local development to use the Vite dev proxy
VITE_API_BASE_URL=
